#
# This null state exists so you can dynamically generate 'include' lists
# which might otherwise be empty and throw an error, without having to
# wrap the include in a potentially complex check for any item existing
# in the list.
